codecov:
  notify:
    after_n_builds: 8
coverage:
  status:
    project:
      default:
        # Don't allow overall project coverage to be dropped more than 2%
        threshold: 2
    patch:
      default:
        informational: true
