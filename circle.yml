dependencies:
  override:
    - python ci/appveyor/driver.py circle install
    - python ci/appveyor/driver.py circle style

test:
  override:
    - python ci/appveyor/driver.py circle build
    - python ci/appveyor/driver.py circle test

  post:
    - python ci/appveyor/driver.py circle after_test
