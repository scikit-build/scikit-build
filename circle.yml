machine:
  python:
    version: 2.7.11

dependencies:
  override:
    - pip install scikit-ci
    - ci before_install
    - ci install

test:
  override:
    - ci before_build
    - ci build
    - ci test

deployment:
  master:
    branch: master
    commands:
      - ci after_test